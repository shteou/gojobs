apiVersion: batch/v1
kind: Job
metadata:
  name: batch-job-23391-15135-29753
  namespace: jobs
spec:
  template:
    spec:
      containers:
      - name: batch-job-23391-15135-29753
        image: perl
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(200)"]
      restartPolicy: Never
  backoffLimit: 1
