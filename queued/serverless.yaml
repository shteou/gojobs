apiVersion: batch/v1
kind: Job
metadata:
  name: serverless-1
spec:
  template:
    spec:
      containers:
      - name: serverless-1
        image: perl
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(200)"]
      restartPolicy: Never
  backoffLimit: 1